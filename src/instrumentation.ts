/**
 * Next.js Instrumentation — OpenTelemetry temporarily disabled
 * Reason: @opentelemetry/sdk-node not installed, commented out to avoid compile errors
 * To re-enable: npm i @opentelemetry/sdk-node @opentelemetry/exporter-trace-otlp-http @opentelemetry/sdk-trace-base
 */

export async function register() {
  // OpenTelemetry temporarily disabled
  // To enable: install the required dependencies and uncomment
}
